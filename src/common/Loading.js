import React from 'react';

const Loading = () => {
    return (  
        <div key="loading" className="spinner-border" role="status">
            <span className="sr-only">Loading...</span>
        </div>
    );
}
 
export default Loading;